<div class="row text-center">
  <h1><%= @set.topic %></h1>
</div>
<div class="row text-center">
  <h3><%= @meme.skill %></h3>
</div>
<div class="row text-center">
  <h3><%= @meme.team_size %></h3>
</div>

<div class="row">
  <h5><%= link_to 'Update a Set', edit_set_path  %></h5>
  <h5><%= link_to 'Delete Set', @meme, method: :delete, data: {confirm: "Are you sure?"} %></h5>
</div>
